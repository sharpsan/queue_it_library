name: flutter_queue_it_example
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  queue_it: any
  flutter_queue_it: any
  faker: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

dependency_overrides:
  flutter_queue_it:
    path: ../
  queue_it:
    path: ../../queue_it

flutter:
  uses-material-design: true
